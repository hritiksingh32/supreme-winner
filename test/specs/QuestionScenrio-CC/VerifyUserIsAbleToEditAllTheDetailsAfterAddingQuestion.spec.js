const utility = require(protractor.basePath + '/test/utility/utilityFunctions.js');
let getAppURL=require(protractor.basePath + '/test/pathFolder/changePath.js');
const resourcePageObj = require(protractor.basePath+'/test/pageObject/resourcePageObj.js');

describe('Verfy that user is able to edit ,remove,Turn of/on the Shuffle Question,Immediate feedback and copy slide in content editor page after adding questions', () => {

    beforeEach(() => {
        browser.ignoreSynchronization = true;
        var Url=getAppURL.ConfigurePath().AppURL;
        var AppendExplore='/explore';
        browser.get(Url+AppendExplore, 40000);
        browser.manage().timeouts().implicitlyWait(30000);
        browser.driver.manage().window().maximize(); 
       
    
    });

    afterEach(() => {
        browser.waitForAngularEnabled(false);
        utility.userLogout();
        browser.manage().deleteAllCookies();
    });
    it('VerifyUserIsAbleToEditAllTheDetailsAfterAddingQuestion',function(){

        utility.handleLocationPopup();
        utility.userLogin('Creator');
        resourcePageObj.createQuestionFITBWithAllDetails();

    });
});